# Vercel Postgres (auto-set when you add Vercel Postgres to your project)
POSTGRES_URL=

# Fallback company name (can also be set in admin Settings)
COMPANY_NAME="Your Company"

# Admin password for the dashboard
ADMIN_PASSWORD=changeme

# Email notifications (optional — uses Resend.com)
RESEND_API_KEY=
EMAIL_FROM="Visitor Log <noreply@resend.dev>"
